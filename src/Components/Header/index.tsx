import styles from "./style.module.css";

const Header = () => {
  return (
    <div className={styles.header}>
      <h2 className={styles.heading}>Color Shades Generater</h2>
      <h2 className={styles.heading}>About Us</h2>
    </div>
  );
};

export default Header;
